/** Tailwind CSS classes */

const classes = {
  mDrawerOverlay: "bg-gray-500 bg-opacity-40 fixed w-full h-full top-0 left-0 z-20",
  mOpenNav: "sm:hidden",
  mDrawerContainer: "w-56 h-screen bg-white fixed right-0 px-5",
  mCloseNav: "fixed top-4 right-4 z-30 cursor-pointer",
};

export default classes;